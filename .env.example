# SQLite database file for contact leads
LEADS_DB_PATH=./data/leads.sqlite

# Resend SMTP for lead notification emails
# 1) Create API key in Resend dashboard
# 2) Verify your sending domain in Resend
# 3) Set SMTP_FROM to a verified sender on that domain
SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=resend
SMTP_PASS=re_your_resend_api_key
# Optional alias (mailer also accepts this as SMTP_PASS)
RESEND_API_KEY=re_your_resend_api_key
SMTP_FROM=SocialMediaMarketing.VIP <onboarding@resend.dev>

# Optional override. Defaults to team@socialmediamarketing.vip
LEAD_NOTIFICATION_TO=team@socialmediamarketing.vip
